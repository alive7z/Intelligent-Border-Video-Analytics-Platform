# Sample Videos

Place local test videos here as:

```
test.mp4
```

## Notes

- Large video files are **git-ignored**.
- Only use small test clips for development.
- Supported formats: `.mp4`, `.mov`, `.avi` (where OpenCV backend supports).
- The pipeline expects `VIDEO_SOURCE=samples/test.mp4` in `.env`.
